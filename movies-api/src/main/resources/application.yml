spring:
  application:
    name: movies-api

server:
  port: 8080
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true

omdb:
  api:
    host: http://www.omdbapi.com
    key: 9b7950e7